# Opening the port gives you more flexibility, there's still a password *AND* Docker's publishing between you and the 'net
include 'postgresql.conf'

listen_addresses = '*'
hba_file = 'pg_hba-docker.conf'
