# The axioms are the base WebHare settings required to bootstrap the generators.

# JSSDK packages we publish to NPM (in their dependency order)
publishPackages: [ eslint-config, ts-esbuild-runner, std ]
