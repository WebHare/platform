wrdSchemas:
  testschema: #autocreated on dev, otherwise CI will be very noise as the testsites get indexed during startup
    schema: mod::system/data/wrdschemas/default.wrdschema.xml
    title: WebHare testsuite test schema
    ifWebHare:
      dtapStages:
        - development
  idp:
    abstract: true
    schema: data/wrdschemas/idp.wrdschema.xml
