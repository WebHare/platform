import type { WRDSingleQueryBuilder } from "@webhare/wrd/src/schema";

// Temporary exports for WH 5.7 compat

export type {
  /** @deprecated Try to avoid for now, if really necessary use \@webhare/wrd/src/schema instead */
  WRDSingleQueryBuilder
};
