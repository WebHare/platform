import type { MapRecordOutputMap, RecordOutputMap, SchemaTypeDefinition, SelectionResultRow, WRDAttributeType, WRDMetaType, WRDTypeBaseSettings } from "@webhare/wrd/src/types";

// Temporary exports for WH 5.7 compat

export type {
  /** @deprecated Try to avoid for now, if really necessary use \@webhare/wrd/src/types instead */
  MapRecordOutputMap,
  /** @deprecated Try to avoid for now, if really necessary use \@webhare/wrd/src/types instead */
  RecordOutputMap,
  /** @deprecated Try to avoid for now, if really necessary use \@webhare/wrd/src/types instead */
  WRDAttributeType,
  /** @deprecated Try to avoid for now, if really necessary use \@webhare/wrd/src/types instead */
  WRDTypeBaseSettings,
  /** @deprecated Try to avoid for now, if really necessary use \@webhare/wrd/src/types instead */
  WRDMetaType,
  /** @deprecated Try to avoid for now, if really necessary use \@webhare/wrd/src/types instead */
  SelectionResultRow,
  /** @deprecated Try to avoid for now, if really necessary use \@webhare/wrd/src/types instead */
  SchemaTypeDefinition,
};
