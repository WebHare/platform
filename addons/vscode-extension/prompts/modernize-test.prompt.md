---
agent: agent
---
Modernize this TypeScript TS file. The 'build-modernize-tests' skill describes how to modernize tests.

Use the `get_errors` tool to find TypeScript/Linting issues

Use the `webhare_runtest` tool to run a frontend/backend test
